version: 1
packages:
  - path: "db"
    name: "db"
    engine: "postgresql"
    sql_package: "pgx/v4"
    schema: "./sql/schema/"
    queries: "./sql/queries/"
    emit_json_tags: true
    emit_empty_slices: true
    emit_pointers_for_null_types: true
    emit_interface: true